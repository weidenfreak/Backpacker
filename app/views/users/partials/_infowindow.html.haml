.row
  .span1
    = link_to image_tag(avatar_url(user, 80)), user_registration_path(:id => user.id)
  .span2
    %strong
      = link_to user.name, user_registration_path(:id => user.id)
    .lead.small
      = user.tagline
      = user.description
